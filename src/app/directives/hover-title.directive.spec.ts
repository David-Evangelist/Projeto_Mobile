import { HoverTitleDirective } from './hover-title.directive';

describe('HoverTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTitleDirective();
    expect(directive).toBeTruthy();
  });
});
